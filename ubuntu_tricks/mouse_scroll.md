# Change Mouse Scrolling Speed on Ubuntu
Follow the following steps:


1. Download imwheel. 
    ```
    sudo apt install imwheel 
    ```
2. Download the shell script [here](./files/mousewheel.sh).
    ```
    chmod +x mousewheel.sh
    sudo ./mousewheel.sh
    # adjust accordingly
    ```
